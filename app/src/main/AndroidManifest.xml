<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- اجازه دسترسی به اینترنت برای سرور و کلاینت -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- اجازه دسترسی به وضعیت شبکه -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- اجازه دسترسی به وضعیت WiFi -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Khoshtip"
        tools:targetApi="31">
        <activity
            android:name=".GameActivity"
            android:exported="false" />
        <activity
            android:name=".JoinLobbyActivity"
            android:exported="false" />
        <activity
            android:name=".HostLobbyActivity"
            android:exported="false" />
        <activity
            android:name=".SinglePlayerActivity"
            android:exported="false" />
        <activity
            android:name=".MultiplayerMenuActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>